# Go
*.test
*.out
/bin/
*.prof
# compiled output of `go build ./testing/wan_bench/` (the cmd
# produces a binary named after the directory, by convention at
# repo root)
/wan_bench
/wan_bench-linux-amd64
# compiled example binaries (named after their dir/package)
/replay
/agent-desktop
/examples/cua-live/cua-live
# cross-compiled wan_bench binary (lives in the package dir, any GOOS/GOARCH)
testing/wan_bench/wan_bench
testing/wan_bench/wan_bench-*
# locally-generated WAN-bench video input
/webrtc-bench/

# JS / TypeScript
node_modules/
# tsc-emitted source maps for the browser examples (regenerated by run.sh)
ts-sdk/examples/compare/*.js.map
ts-sdk/dist/
ts-sdk/node_modules/
js/node_modules/
js/dist/
js/package-lock.json

# editor
.vscode/
.idea/
.DS_Store
.claude/
.devin/
